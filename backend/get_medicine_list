<?php
include 'db_connect.php';

$sql = "SELECT m_id, m_name, price FROM medicine ORDER BY m_name ASC";
$result = $conn->query($sql);

$medicines = [];
while ($row = $result->fetch_assoc()) {
    $medicines[] = $row;
}

header('Content-Type: application/json');
echo json_encode($medicines);
?>
